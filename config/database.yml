default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  encoding: unicode

development:
  <<: *default
  database: route_tracker_db_dev
  username: route_tracker
  host: localhost
  password: 'Asdf1234'

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: route_tracker_db_test

production:
  <<: *default
  database: route_tracker_db_prod
